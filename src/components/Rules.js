import React from "react";

const Rules = ({ closeModal }) => {
  return (
    <div>
      <div onClick={() => closeModal(false)}> Close Modal</div>
      {/* whatshappening */}
    </div>
  );
};

export default Rules;
